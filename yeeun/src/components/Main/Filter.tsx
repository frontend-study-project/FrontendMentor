import Box from '../../styles/Box';

const Filter = () => {
  return (
    <form>
      <Box variant="filter">
        <button>Filter by Region</button>
      </Box>
    </form>
  );
};

export default Filter;
